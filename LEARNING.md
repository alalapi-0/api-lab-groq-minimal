# LEARNING — api-lab-groq-minimal

> 这份文件回答：「我跑完这个仓库，应该真的学到什么？」

## 你跑完应该能回答的问题

1. 为什么大家说 Groq「快」？快在哪一段？
2. 你看到的"耗时"里，**网络往返**、**首 token 等待**、**生成时间** 各占多少？
3. 推理速度对**用户体验**（不是基准跑分）的影响是什么？哪些场景非快不可？
4. 「token/s」、「TTFT」（time to first token）、「端到端耗时」这三个指标分别在量什么？

## 实操验证清单（务必动手）

### 阶段 A — 环境就绪
- [ ] `cp .env.example .env`
- [ ] `pip install -r requirements.txt`
- [ ] 在 Groq 控制台拿 `gsk_...` key
- [ ] 去 Groq 控制台看一眼当前可用模型名，填进 `GROQ_MODEL`

### 阶段 B — 跑通最小调用
- [ ] `python3 main.py` → 重点看终端打印的 **`总耗时: X.XXXs`**
- [ ] `cat output/result.json` → `elapsed_seconds`

### 阶段 C — 速度对照实验（关键）
跑完 Groq 后，再跑一次 `api-lab-openrouter-minimal`，**用同一个 prompt、同一个最大 token 数**，记录两边的耗时。

- [ ] OpenRouter 总耗时 ≈ ___s
- [ ] Groq 总耗时 ≈ ___s
- [ ] 差距是几倍？

### 阶段 D — 拆解耗时（教育性最强的一步）
本仓库故意没做流式（streaming），所以你看到的耗时是"完整等模型说完"。要拆开看：

- [ ] 用 `curl -w '@-' -o /dev/null -s 'https://api.groq.com'` 大致估一下你到 Groq 的网络往返延迟
- [ ] 把 `MAX_TOKENS` 从 80 改成 5，再跑一次 → 总耗时应该变小很多 → 说明"生成时间"是大头
- [ ] 把 `MAX_TOKENS` 改回原来的 80
- [ ] 如果你以后想真正体验"飞快"，应该用流式接口（本仓库故意不做，专注最小闭环）

### 阶段 E — 思考不同地区的差距
- [ ] 如果你在中国大陆直连 Groq，耗时大头几乎一定是网络
- [ ] 如果你换成在海外 VPS 上跑这份代码，耗时应该立刻变小一半甚至更多
- [ ] **这就是为什么"快"是个相对的概念**

## 自检题

1. 当你看到 Groq 端到端耗时 = 1.2s，OpenRouter 端到端耗时 = 6.5s，能不能直接得出"Groq 推理快 5 倍"的结论？为什么不能？
2. 如果一个产品需要"500ms 内返回"，你会优先优化网络（CDN / 区域节点）还是优先换 Groq？
3. Groq 的卖点之一是"高 token/s"，但本仓库的 prompt 只让模型说两句话。这种短输出场景下，Groq 的优势会被放大还是稀释？
4. 流式输出（streaming）能改善"用户主观感受到的速度"吗？为什么？

## 与其它仓库的连接

| 关系 | 仓库 | 为什么去看 |
| --- | --- | --- |
| **同协议、对照速度** | `api-lab-openrouter-minimal` | 用同一个 prompt 跑两边，记录耗时差 |
| **同协议、对照成本** | `api-lab-deepseek-minimal` | 把"快"和"便宜"作为两个独立维度 |
| **本地推理对照** | `api-lab-ollama-local-minimal` | 本地推理通常更慢，但**没有**网络往返——你能立刻明白"耗时不是只看一项" |

## 你应该感受到的"啊哈"瞬间

- 当你看见 Groq 1.x 秒返回，OpenRouter 6 秒返回，但你**没办法立刻断言"Groq 真的快 5 倍"**——你已经具备**了"区分网络与推理"的脑回路**。
- 当你意识到"用户体验上的快"不止依赖模型，还依赖网络拓扑、流式协议、是否有缓冲——你不再把"快"当成一个单一指标。
